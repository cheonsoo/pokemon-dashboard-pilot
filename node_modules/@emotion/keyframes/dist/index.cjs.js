'use strict';

var keyframes = function keyframes(arg) {
  var name = "animation-" + arg.name;
  return {
    name: name,
    styles: "@keyframes " + name + "{" + arg.styles + "}"
  };
};

module.exports = keyframes;
//# sourceMappingURL=index.cjs.js.map
